name: qcarchive-docs
channels:
    - conda-forge
dependencies:
    - python=3
    - sphinx
    - sphinx_rtd_theme
    - nbconvert
    - nbsphinx >=5.0
    - nbformat
    - pandoc
    - pygments
    - jupyter_client
    - ipython
    - pip

      # Depends
    - qcportal

    - pip:
      - git+git://github.com/MolSSI/qcarchive-sphinx-theme#egg=qcarchive_sphinx_theme
